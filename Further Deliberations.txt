Things to try/learn more

Ch 4.9 Declaration, init conversion vs assignment 
{}, (), =
{} - prevents type conversion

Ch 8 Bitwise operation
8.3-8.7
------------------------------------------------------------------------------------------------------------------------------
Ch 10 Ternary operator
result = (condition) ? option1 : option2;
------------------------------------------------------------------------------------------------------------------------------
Ch 12 Looping over array using array elements
------------------------------------------------------------------------------------------------------------------------------
Ch 13 Ptr
Set pointer with address
int * ptr {&val};
Pointer derefencing to get data
cout << *ptr;

Modify char ptr by travesing through the address of char ptr array
ptr of string literal cannot be modified
char * ptr {"string"} is read-only

const char * var = "string" means "string" is a constant too
is string automatically a character array of const type
then const char * var can point to any string

Heap
type * ptr {new type}

Always do
delete ptr
ptr = nullptr

Memory Leaks
- Delete heap but pointer not reset (Dangling Ptr)
- Double allocation to pointer
- Not deleted in scope 

Ch 14 Pointers vs references
const type& {var} - only applies to reference
const type* const p_var{&var} - ptr cannot change var values by deferencing
                            - pointer is fixed
const type* {&var} vs const type* const p_var{&var} 
        - pointer can change where it is pointing in former

Better to pass string by reference

TBD Smart pointers
std::unique_ptr<class> obj = std::make_unique<class>();
------------------------------------------------------------------------------------------------------------------------------
Ch 15 - TBD
------------------------------------------------------------------------------------------------------------------------------
Ch 16 - Functions
constructor is just a function that can be defined multiple times

pass by pointer
function taking pointer
void fnc(type * p_var)
{
    *p_var...; // deference to access
}
call by address
fnc(&var) or p_input

pass by reference
void fnc(type & r_var)
{
    r_var...;
}
call by assignment
fnc(var);

Compiler deals with std::string return type with pointer. It optimizes and uses same address

Ch 16.16-16.24 TBD

Ch 20
Overloading functions -> change input argument

Ch 21 - Lambda functions - anonymous functions
type f_var = [capture list](parameters) -> return type{
    // function body ;
}(self function call);

performs implicit type conversion based on input return type unless return type given
Better to use auto return type if return not specified
auto f_var = [](){}

capture list can take in variables from outside by value and reference
- works like variable declarion of the function
- set to "=" captures everything outside by value
- set to "&" captures everything outside by reference

Ch 22 - Function Misfits
Static var inside function is scoped inside function but has lifetime beyond the call
Inline functions are like a direct copy of the function definition
------------------------------------------------------------------------------------------------------------------------------
Ch 24 - Function Template
Blueprint to generate function - based on input arguments
Helps with writing less repeatative function overloads

Declaration: 
template <typename T>
T function(T var_a, T var_b);

Definition:
template <typename T>
T function(T var_a, T var_b){
    ..;
}

Type specific function call - Template type
- allows template to be different type than input, enforces implicit conversion
function<Type>(input arg);

Pass by reference
Declaration:
template <typename T>
const T& function(const T& var_a, const T& var_b);

Definition:
template <typename T>
const T& function(const T& var_a, const T& var_b){
    a..b..;
}

- Cannot have both pass by value and pass by reference templates defined

- Template vs Overload priority:
1 Raw Overload
2 Closest template type T match
3 Generic template type T match

Template Specialization - add specialization overload

Declaration:
template<>
[return type] function<[template argument specific type]>(input type a, input type b);

Definition:
[return type] function<[template argument specific type]>(input type a, input type b){
    a..b..;
}

For multiple types of input, return type is set by
- use auto for function return type
- decltype to get evaluate return type and trailing return (C++11)

Ch 25 Concepts - constraints on template function

#include concepts
- Template would work only if requirement is fulfilled
- set constraints on template type parameters
- concepts can be type and also type traits (std::is_[type])
- keyword: requires

Concept declaration 4 syntaxes:

After Template Declaration:
template <typename T> 
requires std::[type]<T>
T function(T var_a, T var_b){
    a..b..;
}

In Template Declaration:
template <std::[type] T>
T function(T var_a, T var_b){
    a..b..;
}

In parameter list:
template <typename T> 
std::[type] auto function(auto std::[type] var_a, auto std::[type] var_b){
    a..b..;
}

After parameter list:
template <typename T> 
T function(T var_a, T var_b) requires std::[type]<T>{
    a..b..;
}

Custom concept
- Type trait requirement
- Allows custom action of input parameter to determine a concept

template <typename T> 
concept [custom_concept] = std::[is_type_trait]<T>;

- Argument operation type requirement
- checks for type syntax, not actual operation
template <typename T> 
concept [custom_concept] = requires (T a, T b){
    arg..action();
}

- then usage is same
template <typename T> 
requires [custom_concept]<T>

Nested requirement
- Argument operation evaluation requirement 
template <typename T> 
concept [custom_concept] = requires (arg list){
    arg..action();
    requires arg..action();     // evaluates the action for true
}

TBD
Compound requirement
- check for exception
template <typename T> 
concept [custom_concept] = requires (arg list){
    {arg..action()} -> (optional: noexcept) std::[conversion];
}

Combine concepts
- using boolean operation AND:&& and OR:||
template <typename T> 
requires [concept1]<T> [BOOLEAN_OPERATION] [concept2]<T>        // combined requirement
T function(T var_a, T var_b){
    a..b..;
}

Concept constraint on auto type
std::[type] auto function(auto std::[type] var_a, auto std::[type] var_b){
    a..b..;
}
- Ensure return type by setting concept to auto var
std::[type] auto var = func();

------------------------------------------------------------------------------------------------------------------------------
Ch 26 - Class

Class structure
class [class]{
    public/protected/private:   // members are private by default
        members
        function
    
    public:
        // Called based on argument passed when multiple constructors present
        // same name as class
        // can also be a template function with concepts
        [class](type arg){
            member = arg;
        }
}

// Setting default empty constructor in case there is a constructor
but want to keep default constructor behavior
[class]() = default

[class] obj;                    // calls by default empty constructor
[class] obj(arg);               // calls defined consturctor based on arg
[class] obj = [class](arg);     // calls defined consturctor based on arg
[class] obj{arg};               // calls defined consturctor based on arg, no type conversion

External access to public class members:
[class]::member

External setup to class function/constructor:
[class]::[class](type arg):[initializer list]{
    ...
}
[type] [class]::function(type arg){
    ...
}

Initializer list:
member{arg},member{arg}...

Class pointers - stack
[class] obj(args);
[class] * p_obj = &obj;

Dynamic memory class pointer - heap
[class] * p_obj = new [class](args);    // use {arg} to avoid type conversion

Member access
(*p_obj).member - derefencing
p_obj->member - no need to dereference

delete p_obj
p_obj = nullptr

Class Destructor
~[class]()
- Release dynamic memory
- Setting variables to null

Destructor is called when:
- local stack goes out of scope
- heap object is released using delete
- Object passed by value to function - reduces memory use
- Object returned by function - for some compilers

Copy constructor - Need to learn
Test pointer member in class copied for function call

Constructor and Destructor call order
constructor called in ascending order and destructor called in descending order

"this" pointer - contains address of class obj
- hidden pointer
- manipulater current obj
- used from inside the member function
- can be used to return the pointer to obj

- helps solve conflicting member variable name scenario
this->[member] = member;

- chained calls using pointer
[class] * p_obj = new [class](args);    // use {arg} to avoid type conversion
p_obj->func1()->func2()->func3;

[class] * func1{
    return this;    // return pointer
}

- chained calls using reference
[class] * p_obj = new [class](args);    // use {arg} to avoid type conversion
p_obj->func1().func2().func3;

[class] & func1{
    return *this;   // return dereference pointer
}

Reference obj - will auto remove if one is deconstructed
[class] & r_obj = new [class](args);
r_obj.func1().func2().func3;

[class] & func1{
    return *this;   // return reference
}

Struct
- members are public by default, unlike class
- no function

Size of class objects
- accounts for size of member variables

"explicit" constructor prevents single arg consturctors to be used for implicit conversions
------------------------------------------------------------------------------------------------------------------------------
Ch 27 - 35 TBD
------------------------------------------------------------------------------------------------------------------------------
Ch 36 Inheritence
note: By Inheritence we can only make the derived class constrained, not relaxed
Base class constructor called first, in order of heirarchy

define [base class]
class [base_class];   // forward declaration?

class [derived_class]: [Inheritence_Contstraint] [base_class]{
};

[Inheritence_Contstraint] = public|protected|private

friend class/function
- has access to private members of derived class
- no access to base class

public Inheritence - derived class will have access to public and protected members
(no change in derived class member access design)
public -> public    // public members will be accessible from outside
protected -> protected
private -> private

protected Inheritence - derived class will have access to public and protected members
public -> protected
protected -> protected
private -> private

private Inheritence - derived class will have no access to base class members
public -> private
protected -> private
private -> private

Public and protected members can be accessed by member functions and/or directly

Base Class  |   public Inheritence  | protected Inheritence |   private Inheritence
-----------------------------------------------------------------------------------
public      |   public              |   protected           |   private
protected   |   protected           |   protected           |   private
private     |   private             |   private             |   private

class           |   public              |   protected               |   private
access          |   object/external     |   only class function     |   only class function
inheritence     |   object/external     |   only class function     |   only base class function


Resurrection
code in derived class
using [base_class]::[member]

Chain constructor call for base class within derived class consturctor
[derived_class]:[derived_class](type arg):[base_class](arg), [member](arg)

Copy consturctor for copying class by reference than using by value
[base_class]::[base_class](const & [base_class v_class]){
    ...;
}

[derived_class]::[derived_class](const & [derived_class] v_class):[base_class](v_class), member{v_class.member}{
    ...;
}

Inherit Base constructor by ressurection
using [base_class]::[base_class];       // Inheriting constructor

To reuse symbols through inheritence:   // for base and derived in case of same member name
[derived_class] [d_obj];
[d_obj].[member] = ...;
[d_obj].[base_class].[member] = ...;

Ch 37 - Polymorphism - Store class objects in collection
Base pointer holding a Derived object

Static binding:
- Use pointer to setup object
- Will need a function that takes in object pointer/reference and calls obj.func()

[derived_class] d_obj;
[base_class] * p_obj = &d_obj;
[base_class] & p_obj = d_obj;

Dynamic binding: Virtual functions      // looks at type of object calling and uses their func
Calls function of object

Declaration and Definition:
virtual function(){
    ...
}

[derived_class] d_obj;
[base_class] * p_obj = &d_obj;
p_obj->v_func();

[base_class] & p_obj = d_obj;
p_obj.v_func();

function([base_class] * obj){
    obj->v_func;
}

function([base_class] & obj){
    obj.v_func;
}

Slicing
[base_class] obj = [derived_class]();